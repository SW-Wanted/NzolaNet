# TMS — Project Memory

> Auto-generated by TM Code. Last updated: 2025-01-16
> This file is read by the AI agent at the start of each session.

---

## Project Analysis

### Overview
- **Name:** frontend
- **Framework:** Angular 21 (SSR enabled)
- **Language:** TypeScript
- **Package Manager:** npm@11.14.1
- **Monorepo:** no

### Commands
- **Install:** `npm install`
- **Dev:** `ng serve` (or `npm start`)
- **Build:** `ng build`
- **Test:** `ng test` (uses Vitest)
- **Lint:** not configured

### Structure
- `src/` — main source directory
  - `app/` — Angular app root (config, routes, main component, specs)
  - `pages/` — UI mockups and designs (HTML screenshots, wireframes for various screens: feed, profile, groups, market, etc.)
  - `index.html`, `main.ts`, `main.server.ts`, `server.ts` — entry points
  - `styles.css` — global styles
- `public/` — static assets (favicon)
- Root config: `angular.json`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.spec.json`

### Conventions Detected
- Standalone components (Angular 17+ modern pattern)
- Strict TypeScript with strict templates
- Prettier formatting (printWidth: 100, single quotes, angular HTML parser)
- Vitest for unit testing (not Jest)
- Server-side rendering via `@angular/ssr` + Express
- No ESLint configured

### Dependencies (Key)
- `@angular/core`, `@angular/common`, `@angular/router`, `@angular/forms` — framework core
- `@angular/ssr` — Angular universal/server-side rendering
- `express` — SSR server runtime
- `rxjs` — reactive programming
- `vitest` + `jsdom` — unit testing
- `prettier` — code formatting

---

## Memory

### Milestones
(empty — will be filled as work progresses)

### Decisions
(empty — will be filled as architectural decisions are made)

### Pending Tasks
(empty — will be filled as tasks are identified)

---

## Custom Instructions

Add your project-specific instructions below. These will guide the AI agent.